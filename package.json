{
  "devDependencies": {
    "@types/chrome": "^0.1.42",
    "esbuild": "^0.28.0"
  },
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  }
}
